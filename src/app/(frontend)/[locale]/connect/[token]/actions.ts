'use server';

/*
 * Connecting by scanned badge was withdrawn; `page.tsx` in this folder
 * redirects. The file remains only so the route group has no dangling
 * import, and holds nothing.
 */
export {};
